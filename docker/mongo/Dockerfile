# syntax=docker/dockerfile:1.7

FROM mongo:8.2-noble

LABEL org.opencontainers.image.title="CodeMetrics local MongoDB"
LABEL org.opencontainers.image.description="Local MongoDB image for CodeMetrics development only. Production uses MongoDB Atlas."
